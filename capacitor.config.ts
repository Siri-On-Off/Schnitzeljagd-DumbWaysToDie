import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Schnitzeljagd-DumbWaysToDie',
  webDir: 'www'
};

export default config;
